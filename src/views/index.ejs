<%- include('includes/head') %>
<%- include('includes/nav') %>

<% if(user) { %>
    <div class="container">
        <div class="row">
            <div class="col-lg-2"></div>
            
            <div class="col-lg-8 my-3">
                <h1 class="text-center">Contact Agenda</h1>
                <p class="text-center lead">Hello, <%= user.email %></p>
                <%- include('includes/messages') %>
                
                <div class="text-center mb-4">
                    <a href="/contacts" class="btn btn-success">New Contact</a>
                </div>

                <div class="table-responsive">
                    <table class="table table-hover my-3">
                        <thead class="table-dark">
                            <tr>
                                <th>Name</th>
                                <th>Surname</th>
                                <th>Email</th>
                                <th>Phone</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if(contacts.length > 0) { %>
                                <% contacts.forEach(contact => { %>
                                    <tr>
                                        <td><%= contact.name %></td>
                                        <td><%= contact.surname %></td>
                                        <td><%= contact.email %></td>
                                        <td><%= contact.number %></td>
                                        <td>
                                            <a href="/Contacts/index/<%= contact._id %>" class="text-primary mx-2 text-decoration-none">
                                                Edit
                                            </a>
                                            <a href="/Contacts/delet/<%= contact._id %>" class="text-danger mx-2 text-decoration-none">
                                                Delete
                                            </a>
                                        </td>
                                    </tr>
                                <% }) %>
                            <% } else { %>
                                <tr>
                                    <td colspan="5" class="text-center text-muted py-4">
                                        No contacts found.
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
            
            <div class="col-lg-2"></div>
        </div>
    </div>
<% } else { %>
    <div class="container">
        <div class="row justify-content-center my-5">
            <div class="col-lg-6 text-center">
                <h1 class="mb-4">Welcome</h1>
                <p class="lead">Please log in to your account to view your contacts.</p>
                <%- include('includes/messages') %>
            </div>
        </div>
    </div>
<% } %>

<%- include('includes/footer') %>